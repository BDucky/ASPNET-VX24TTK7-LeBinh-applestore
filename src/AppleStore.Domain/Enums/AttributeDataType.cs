namespace AppleStore.Domain.Enums;

// Ordinal values inferred from the ProductAttributeValues.ValueText/ValueNumber
// split; not explicit in the source report.
public enum AttributeDataType
{
    Text = 0,
    Number = 1,
}
