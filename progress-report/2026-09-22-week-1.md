# Progress report: week 1 (2026-09-17 to 2026-09-22)

Individual project, TVU Cong nghe phan mem, 2025. Repo:
`BDucky/ASPNET-VX24TTK7-LeBinh-applestore`.

## What landed this week

**2026-09-17, session 1: repo scaffold and M1 start.**
- Solution scaffold: `AppleStore.Domain`, `AppleStore.Infrastructure`,
  `AppleStore.Web`, `AppleStore.Tests`, all 24 entities from the schema,
  EF Core configuration, initial migration.
- `main`/`dev` branch model set up; `dev` is the integration target going
  forward (see `docs/branching.md`).
- M1 (Auth and OTP), first slice, test-first: `OtpService` and
  `RegistrationService`, 10 tests, PR #2 (`feat/register-account` into `dev`).
- Verified: build, full test suite, a real SQLite migration applied, app
  driven in a headless browser (`docs/verification.md`).

**2026-09-22, session 2: course submission requirements.**
- Transcribed the course brief's GitHub-management requirements (repo
  naming, instructor collaborator, README upkeep, weekly commits,
  progress-report folder, directory tree) into `docs/submission.md`.
- Repository renamed to the required syntax:
  `ASPNET-VX24TTK7-LeBinh-applestore`.
- Required top-level directories scaffolded: `setup/`, `scr/`,
  `progress-report/`, `thesis/{doc,pdf,html,abs,refs}`.
- Root `README.md` updated with the repository-organization and team-contact
  sections required by the brief.

## Still open

- Instructor (`antonio86doan@gmail.com`) not yet invited as a collaborator;
  needs their GitHub username, see `docs/submission.md` section 4.1b.
- Team contact roster (name, email, phone per member) still needed for
  `README.md`; only one member's contact is filled in so far.
- Branch protection on `main`/`dev` requested but not configurable without
  repo admin, see `docs/branching.md`.
- M1 remaining pieces not started: register/verify-OTP controller and views,
  log in, forgot password, change password, update profile
  (see `docs/roadmap.md`, M1 table).

## Next

Push the `docs/github-submission-requirements` branch and open its PR into
`dev`, then continue M1 with the register/verify-OTP UI.
